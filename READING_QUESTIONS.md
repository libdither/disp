Questions on things that might be interesting to do:
 - How feasible would it be to unify functions with multiple inputs with records and allow both of them to have fields that are inferred from other fields? *(LANDED 2026-06-11: telescopes — derived fields pinned by tree_eq, δ via the `Reduce` Action arm, record-type literals, `mk` builder; see `KERNEL_DESIGN.md` § Telescopes + `lib/tests/telescope.test.disp`. Unification-style inference deferred; the entry encoding carries the solver slot.)*
 - I'm thinking about the `arg.func` syntax and how intuitively it just means "call this `func` function on this argument `arg`" where in typical languages its the first argument or some kind of special argument (i.e. `impl`) in rust. But I'm wondering if this can just be turned into a general "inverse apply", that drops out `coproduct.product` as well. I imagine here then coproducts would have to be the things that hold the selector algorithm, not the product. Does this work? And if so, is it a more general solution? Does it meld nicely with inference as well? (i.e. I'm imagining doing name-resolution as a kind of inference perhaps?)
 - Can wait-forms be represented as just plain sets (or perhaps we need a generalization of set that enables arbitrary waiting?), resulting in unification between neutrals and a lot of other things? Would walker then just be like "certain fields in this particular set are private and can't be accessed?". Thoughts on this?
 - Reading through cut.disp/engine.disp (was core.disp), I notice a lot of like re-creation of wait wrappers (i.e. checker_sig?), and requirements to like store sig in a separate term, which confuses me because shouldn't the term itself double as the signature? (Is there a reason it is this way? If not, could it be streamlined / made more elegant) *(ANSWERED 2026-07-07: the term does double as the signature. A sig is a projection of the term (pair_fst of the wait fork), not separate storage; checker_sig exists because pair_fst gives the checker inside its partial-application wrapping, so a known checker must be wrapped the same way before comparing, and the named sigs (hyp_sig, cut_sig) are cached projections that hash-consing makes O(1). Not identity, verified empirically. Answer recorded at checker_sig in cut.disp; pins in lib/tests/checker_sig.test.disp.)*
 - Effects to guarantee termination? Asynchronous

Smaller writing/naming questions:
 - How much "AI smell" is there in the various documents and comments? How tedious/fluffed is it to read? Could we make a list? *(SURVEYED 2026-07-07 — the list, worst first:)*
   - *Density numbers (em-dashes / lines):* CLAUDE.md 103/102, OPTIMIZER.typ 142/768, EVALUATOR.md 16/100, NEGATIVE_TYPES.md 31/205, KERNEL_DESIGN.md 31/269, TYPE_THEORY.typ 537/5710, SYNTAX.typ 22/422. Clean already: README.md 0/328, MODULES.md 2/467, GOALS.md 3/22. Bold-pair density: EVALUATOR.md 32 pairs in 100 lines, NEGATIVE_TYPES.md 50 in 205.
   - *The recurring smells:* (1) clause chains spliced by dashes instead of sentences; (2) CAPS-for-emphasis in prose (NOT/ONE/IS/LANDED/RETIRED); (3) boldface as a substitute for structure; (4) status-log accretion, dated "(landed …)" parentheticals layered into design prose until it reads as a changelog; (5) coined-codename density (the tree_rec rung, fragment -1, R6, the §7A token) that forces cross-referencing; (6) repeated verdict framing ("the one X", "exactly the Y") losing force through repetition.
   - *Suggested order of attack:* EVALUATOR.md (entry-point doc, 100 lines, quick win), NEGATIVE_TYPES.md (move status parentheticals into one status block), OPTIMIZER.typ, then TYPE_THEORY.typ opportunistically per touched section. CLAUDE.md is machine-context and may be exempt on purpose.
 - Can we investigate TODOs in the code and see what the resolution might be? *(DONE 2026-07-07: every in-code TODO/Q from the read-through resolved or answered in place — Return alias deleted, annihilate renamed to cut, make_meta_f and the meta accessors deleted, TT/FF renamed true/false (representation question stays open, noted at the definition), test failures report file:line, sum literals auto-bind constructors, checker_sig/type_meta/polarity/coproduct-syntax Qs answered in cut.disp comments. Remaining open on purpose: the monad-library Q at bind in cut.disp, and the Bool representation experiment.)*

