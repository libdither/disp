-- Prelude: typed standard library for Disp.
-- Loaded after types.disp. All definitions type-checked.

-- Typed Bool constants (tt/ff are untyped and carry OPAQUE, which poisons
-- S-node D annotations in typed bracket abstraction).
let bTrue : Bool := leaf
let bFalse : Bool := leaf leaf

-- Boolean operations
let not : Bool -> Bool := {b} -> triage false ({_} -> true) ({_ _} -> false) b

-- Polymorphic identity
let id : (A : Type) -> A -> A := {A x} -> x

