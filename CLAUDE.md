# Disp — Claude Code context

Dependently-typed language built on tree calculus. Types are predicates; the type checker is a tree program; the object language is meant to be fully self-hosting.

## Read before making design changes

- [`GOALS.md`](GOALS.md) — the north star (neural-guided synthesis, self-improving optimizer) and the metacircular discipline used to get there.
- [`TYPE_THEORY.typ`](TYPE_THEORY.typ) — **authoritative** type-theory spec (target). **Two** Σ-operations (`hyp_reduce`, `bind_hyp`) plus the two-argument dispatcher `param_apply` over a *fixed* Σ that routes pinned sigs to the *registered* handler (Σ is a routing table, not just a trust set — `param_apply f x` runs `h (wait_meta f) x` for the registered `h`, ignoring `f`'s embedded handler). `eliminator_frame` is demoted to a library `elim` over `hyp_reduce` + a type's `respond` meta-field. `postulate` is **removed** — effects are now an entirely-library construction: a free monad `Eff R X` over an operation signature, interpreted by deep handlers, with one impure *driver* at the program boundary (§15); the substrate's purity forces effects to be values, never dispatch targets. Walker protection is one set: `seal(Σ)` (trusted-token producers — `hyp_reduce`; unforgeable *and* uninspectable), `forge(Σ) = seal(Σ)` (the `funnel` set dissolved with host effects); `bind_hyp` is in neither (its invocations are built by library recognizers under the walker). `respond` is a *constitutive* (non-optional) meta field, typed `RespondShape` and checked by `StrictType`/`BehavioralType`; the two-tag `Action` (`Extend | Reduce`) with rejection = `Extend InvalidType`. Manifest contracts over the tree-calculus substrate; `CheckerResult` monad with named-variant errors; library types carry MetaShape-conforming meta records (`recognizer_params`, `functor`, `respond`, `behavioral_specs`); cubical operations live in each type's `functor` meta-field; validators (`Type` / `StrictType` / `BehavioralType`) are ordinary library wait-forms.
- **Legacy spec — removed; in git history** (was `archive/TYPE_THEORY_LEGACY.typ`; recover via `git log --all --diff-filter=D -- archive/TYPE_THEORY_LEGACY.typ`). The previous shape had seven kernel primitives (`hyp_reduce`, `guard`, `unguard`, `checked_apply`, `predicate_frame`, `eliminator_frame`, `bind_hyp`); the codebase has **migrated off** it to `TYPE_THEORY.typ`'s two-Σ-op kernel — see "Implementation status" below.
- [`SYNTAX.typ`](SYNTAX.typ) — surface grammar and AST shape. Authoritative for the parser.
- [`COMPILATION.typ`](COMPILATION.typ) — parse/elaborate/emit pipeline.
- [`KERNEL_DESIGN.md`](KERNEL_DESIGN.md) — tree-calculus implementation idioms for the current two-Σ-op kernel: `hyp_reduce`/`bind_hyp`/`param_apply`, wait/fix, signatures, neutrals, the `tree_eq` native fast-path, bracket-abstraction caveats.
- [`NEGATIVE_TYPES.md`](NEGATIVE_TYPES.md) — design rationale for the telescope (the one negative former for `Pi`/`Sigma`/`Record`/`⊤`): the forced-choice chain that nearly determines it, the observation-interface/NbE ideal it approximates, and the frontier of remaining improvements. The one architectural move — unifying recognition+respond into a single mode-polymorphic walk (`tele_walk`) over **wait-form cells** — **landed (2026-06)**. `qid`→Intersection **also landed (2026-06-29)**: Intersection/Refinement/Eq are now telescope HYBRIDS (telescope recognizer over `qid`/`mint`/`refine`/`imeet`/`eqends` cells + a type-specific respond), and `GoodRespond` is a telescope of `probe` cells (so `verify_good` composes under the walker) — all new ops pluggable with no `tele_walk` edit. The live frontier is now frame-tagging for mixed/callable records. Read before reworking the telescope `tele_walk` walker / its cell ops.
- [`EVALUATOR.md`](EVALUATOR.md) — the reduction-backend subsystem map: the `Session` ABI, the five backends (disp-eager / naive / rust-eager / rust-ic-net / lambada peers) and how they relate (strategy 1 vs 2, checker-vs-optimizer-vs-bench, the differential-oracle discipline). Read before touching `src/eval/` or a backend crate; detail in `EVALUATOR_PLAN.md`, calculus in `research/interaction-combinator/tc-net.typ`.
- [`OPTIMIZER.typ`](OPTIMIZER.typ) — **unified design** for the verified self-improving optimizer (the `GOALS.md` endgame): architecture (untrusted optimizer + trusted certificate checker + `φ`), cost-as-a-graded-coeffect, the staging axis (memo→AOT as one verified rewrite), equality/licensing (the walker-defined `~_T` + Sands improvement), superposition-as-reverse-mode search, the low-level/effects frontier. Folds the former 4-doc research stack + DISP_BACKPROP (git-recoverable). Not yet built; substrate (`rust-ic-net`) mostly exists.

## Core discipline

**The object language is the specification. Host implementations are optimizations.**

Every component participating in checking, elaboration, or conversion must have a declared tree-calculus encoding. The TypeScript runtime (`src/core/tree.ts`) is the only host code; everything else — type predicates, the parametric walker, the elaborator — are tree programs. A native fast-path in `src/core/tree.ts` must mirror in-language code semantically (bit-identical results) and be validated against the in-language reference. Currently the only live native fast-path is `tree_eq`; the dispatcher (`param_apply`) runs in-language. (A native dispatcher fast-path existed for the legacy kernel but was orphaned and removed in the cutover; re-introducing one requires restoring an equivalence test.)

## Code layout

### Source
- `src/core/tree.ts` — tree calculus runtime: hash-consed trees, eager iterative `apply`, `tree_eq` native fast-path. (The dispatcher/parametric-walker runs in-language via `param_apply` in `lib/kernel/engine.disp`; there is no native dispatcher fast-path.)
- `src/parse.ts` — tokenizer / parser. Implements `SYNTAX.typ` grammar.
- `src/run.ts` — file runner: loads `.disp`, parses, compiles, executes tests (failure printer decodes via the Session ABI on handle backends).
- `src/compile.ts` — the elaborator's public surface, a re-export barrel over `src/elab/`:
  - `elab/state.ts` — shared session/budget/caches + `ScopeEntry`/`SigParam`/`CompileSinks` + `collectSessionRoots`.
  - `elab/cir.ts` — CIR + bracket abstraction (η-reduction + K-composition optimizations); part of definitional equality.
  - `elab/sugar.ts` — surface rewrites: select_lazy→if, named/default/reorderable args, `binderToPi`.
  - `elab/literals.ts` — Nat/String/accessor encoding + record-header decoding.
  - `elab/expr.ts` — `exprToCir` (the one fold over the surface AST) + `compileExpr`/`compileType`.
  - `elab/driver.ts` — `parseProgram`: scope stack, `use`/`use raw` + per-session module cache, deferred auto-verification, `tree_eq` native-fast-path registration, sum-literal constructor auto-declaration (`X := < Tag : T, … >` also binds `Tag := inj "Tag"`/nullary `inj "Tag" t`, skip-if-bound; SYNTAX.typ, pinned in sum_ctors.test.disp).

### Library layout (`.disp` files in `lib/`)
- `prelude.disp` — fundamental combinators (true/false Scott-encoded, triage, select, pair, wait/fix, tree_eq, nat_le, zero/succ/nat_pred). **Fragment -1 of the bootstrap (2026-07-07):** a GIVEN-BEARING module — its annotation type names (Type/Tree/Bool/Nat/Pi) plus the auto-verification formers are givens; consumers below the kernel load it `open use raw "../prelude.disp" {}` (values only), and the barrel's final checked re-import fills the givens so the annotations (true/false/test/triage/pair/tree_eq/zero/succ) compile and verify through the kernel. Never open it bare (bare use of a given-bearing module is the functor face); files that open the kernel barrel don't need it at all (the barrel re-exports). The raw dispatchers (pair_fst, is_fork, …) stay untyped: their checks would triage an abstract Tree (typed twins ride `tree_rec`, see tree_rec.test.disp); `wait`/`omega`/`fix` are typed at Tree via Tree's application face (the tree_rec rung, 2026-07-07).
- `kernel/` — the two-Σ-op kernel center, split into seven fragments assembled by `prelude.disp` in value-dependency order (annotations may forward-ref any fragment via the raw layer; **values may not** — a fragment only references values from earlier fragments):
  - `prelude.disp` — canonical entry point + bootstrap **barrel**: raw-opens `../prelude.disp` (fragment -1), then raw-loads all seven fragments (values, no annotation processing) and re-imports them checked **with explicit fill records** (MODULES.md): each fragment declares its annotation-forward type names as `given`s, the raw pass builds the fill environment, the checked pass instantiates. The barrel's LAST open is the prelude's checked instantiation (fills its givens with the kernel's types + the verification formers), which activates and verifies the prelude's annotations; the open-dedupe (raw ≡ checked per name) is the bootstrap fixpoint condition. Fragments are HERMETIC as of 2026-07-06 — each raw-opens `../prelude.disp` + raw earlier siblings itself (value deps are acyclic); givens cover the genuinely mutual annotation types (incl. in-file self-forwards like base's `Eq`/`Unit`, universe's `Type`, filled from the same raw pass). Files that build on the type system do `open use "../kernel/prelude.disp"`.
  - `cut.disp` — **the §2.6 value cut:** `prod`/`cut`/`proj`/`inj`/`acc`/`make_record`/`field` + `type_meta`/`checker_sig`; the list toolkit (`list_nth`/`list_index_of`/`list_update`/`list_rev`/`list_cat` — the one home for cons-list folds, since `let`s don't cross fragments); control coproducts `Action` (TWO arms: a neutral elimination stays stuck `Extend type` or computes `Reduce value`, both bare — the verdict case is spelled `Reduce (Ok v)`, no alias) / `CheckerResult` (`Ok | Err`); `make_meta` (MetaShape builder); the declaration protocol (COMPILATION.typ § Declarations as requests): `Bind`/`Install`/`Both` (GuardAction constructors), the request decorators `base`/`let`/`sig`/`guard` (`let` IS the surface `let` — not a keyword; `let x := e` is a decorated declaration), and `default_guard` (normative — the elaborator's declaration fast path mirrors it and is taken only while the ambient default is pristine; `let` gets the same pristine-capture fast path). Records (metadata, `recognizer_params`, neutral-meta `{stored_type; payload}`, eliminator frame `{motive; cases}`) are read **by name** through the cut (`.field`).
  - `engine.disp` — **the checker engine:** Σ-ops `hyp_reduce` (push a frame onto a neutral via the stored type's `respond`) + `bind_hyp` (merged into the walker: mint, body-walk under `self`, `occurs` escape-check); the recognizer shim `make_recognizer`; the dispatcher `param_walker`/`param_apply` (reader carve-outs `pair_fst`/`neutral_type`/`I`/`tree_eq` + Σ routing on `hyp_sig`/`bind_hyp_sig` + the §7A `apply_policed` token); `InvalidType`; boundary `typecheck`/`verify : Module -> CheckerResult`; `elim`; the respond combinators `inert`/`inductive`/`gated_inductive_respond`. **Typing discipline:** every export is either ANNOTATED (machine-verified at module check) or marked `SEALED(<why>)` — constitutively untypeable because its body is the reflection/forging the walker rejects; see the file header.
  - `cells.disp` — **wait-form cells + the walker + the negative formers.** A telescope is `t cell (λx. rest)`; a **cell is itself a wait-form** `wait op meta` — inspectable (signature = which observation, meta = its data) AND runnable. Ops are mode-polymorphic and return a **Step** (`SMint | SThread | SReject | SDone`, exported — the vocabulary userland ops speak, with `do_check`); ONE walker `tele_walk rs mode tele source frame prior` serves both faces (`tele_walk true` recognize, `tele_walk false` respond) and interprets Steps with the recursion + `bind_hyp` INLINE (op = per-cell algebra, `tele_walk` = harness; `bind_hyp` must stay in `tele_walk`, see § Compiler workarounds). New observation modes plug in as new ops with no `tele_walk` edit. Cell constructors `mint_cell`/`apply_cell`/`proj_cell`/`derive_cell`/`pos_cell` + meet cells (`qid`/`refine`/`imeet`/`eqends`) + recursion cells (`rec`/`gen`/`rec_at`, markers `Rec`/`RecUnder`/`RecAt`/`Param`) — the elaborator (recType desugar) emits the same ones, so surface `{a:Nat}` == manual `Telescope (proj_cell "a" Nat)`. `tele_with cells respond` is THE shared wait-form shape (telescope recognizer + given respond); `Telescope` is the negative instance (`tele_walk false` respond), **guard-free** (`Telescope t` = the empty meet = ⊤ = `Tree`); `Pi`/`Sigma` are thin instances; `Record` lifts its flat field list **lazily** (an eager lift would trip `Record : Tree -> Type` self-verifying). `Pi A B` = `[mint x:A ; apply out:(B x)]`; `tele_walk false` routes by the lead cell — mint-lead instantiates the binder and `Extend`s the codomain, else projects. So `Π`/`Σ`/records/`⊤` are one negative former differing only in cell op. See `KERNEL_DESIGN.md` § Telescopes.
  - `base.disp` — **first-order library types:** Unit, String, False/Not, Eq (+ `eq_J`/`eq_subst`/`eq_sym`/`eq_cong`/`eq_trans`), Refinement, Intersection (both `tele_with` hybrids). (The recursors live in `generic.disp`; the builders in `lib/std/build.disp`; HBin in `lib/std/hbin.disp`; Ord's constructors in `positive.disp`.)
  - `positive.disp` — **the positive former + the coherence gate + Bool/Nat/Ord.** `Coproduct` (a sum) is the dual of the telescope and NOT one: per-variant positional telescopes (`pos_tele`) recognized by the SAME `tele_walk`. **Recursion/corecursion are cells (Path B):** a recursive constructor arg is `Rec` (= `RecUnder Id`) or `RecUnder F` for NESTED recursion; the former ties its OWN knot (`make_rec_recognizer`, rs=self); mutual recursion is `Coproduct_ctx` + `RecAt i`; codata is `Stream` (`std/stream.disp`, a `gen` cell). `Mu`/`Mu_resp` are RETIRED. The η-readback coherence gate `coh_check` lives in the RESPOND (`gated_inductive_respond`), so every `Coproduct`/`Coproduct_p`/`Coproduct_viewed` inductive is gated. ONE maker: `Coproduct_viewed_respond params respond iso variants` (every sum is viewed; inj-tagged sums use the identity iso) — `Coproduct`/`Coproduct_p`/`Coproduct_respond`/`Coproduct_viewed` are one-line specializations; `Bool`/`Nat`/`Ord` (+ their constructors `zero_ord`/`omega_plus`) are `Coproduct_viewed` instances. `derive_case_telescope` = the recursor's case-record type.
  - `generic.disp` — **the cell-derived generics** (one implementation for every inductive, read off the cells): `fold_value` (catamorphism), `rec_value` (dependent recursor via `elim`; decodes through the `functor` view, so shape-encoded types dispatch by variant tag) + the kernel recursors `nat_rec`/`bool_rec`/`ord_rec` as its annotated one-line instances, `children` (traversal), `fmap`/`bimap`/`fmap_n` (positional optics over `Param` markers) + record-side `key_over`/`fmap_rec`/`bimap_rec`.
  - `universe.disp` — **the metacircular universe + structural types + GoodRespond:** `StrictType` (a `tele_with` hybrid: two-qid-cell recognizer + `type_predicate_h_rule` respond; inhabits itself); **`BehavioralType` = `Type` (R6, landed 2026-07-01)** — StrictType + GoodRespond's obligations MERGED as a dependent tail: gated inductives with concrete variants get (mint abstract motive/self/cases; probe coh = `Extend (M s)`), and responds that CLAIM to gate (`respond ≠ inductive_respond`) also get per-variant junk probes (`Extend InvalidType`); declared-ungated types skip them (visible category, defended downstream). Tractable everywhere because `do_check` routes membership through `param_apply` — the §7A token flattens nested checks to fresh native walks; the former every-backend OOM is gone (pinned in `behavioral_type.test.disp`). Also the kernel's own structural types `Tree`/`Frame`/`MetaShape`/`NeutralMeta`/`Module`/`Action`/`Step`/`RespondShape` (+ per-former shapes). **Tree is the floor AND an inductive (the tree_rec rung, 2026-07-07):** membership stays the O(1) `Ok true` recognizer, but Tree's meta carries variants `[leaf | stem Rec | fork Rec Rec]` + the total view iso + an APP-OR-ELIM respond — a `{motive; cases}` frame is a gated elimination (so the kernel exports the dependent recursor `tree_rec`, and typed twins of the raw dispatchers become writable), any other frame answers `Extend (neutral_type self)` (a tree applied to a tree is a tree — this is what types the prelude's `wait`/`omega`/`fix`). Frame-shape dispatch over-rejects but never unsounds (pinned in `tree_rec.test.disp`). Plus `Neutral` (the type of stuck values — a recognizer WITHOUT make_recognizer's nominal H-rule shortcut, which no other type can admit) and `Spine` (a neutral's payload: `Mint id | Ext parent-NEUTRAL frame`; storing the parent neutral is how `occurs` sees through spines, and why an extended payload is never closed — membership goes through `param_apply`, not `typecheck`) and `GuardAction`/`Request` (the declaration protocol's shapes); `GoodRespond`/`verify_good` (the standalone boundary checker); typed meta access `meta_frame`/`meta_of : Type -> MetaShape` (the `@meta` arm of `type_predicate_h_rule`; abstract types answer a stuck, well-typed meta whose projections land their field types — `{T} -> (meta_of T).respond : Type -> Tree` checks, pinned in metashape.test; the former per-field accessor exports are deleted).
  - The generic walker costs ~2× the old specialized ones (`APPLY_BUDGET` in `src/compile.ts` is 40M); a validated lean fast-path could reclaim it.
- `elab/` — **REMOVED 2026-07-01** (was the self-hosting elaborator, `ELABORATOR_PLAN.md` Stages 0–3: `bracket.disp`/`ast.disp`/`compile.disp` + their `lib/tests` and host cross-validation tests). It was a validated spec mirror with no live consumer; deleted until the language is mature enough to do it justice. Recover via `git log --diff-filter=D -- lib/elab`. Until then the host elaborator (`src/compile.ts`) is the only pipeline — bracket abstraction is still part of definitional equality, so changes there need extra care (no in-language oracle at the moment).
- `std/` — standard library on top of the kernel (`open use "../kernel/prelude.disp"`): `nat/arith.disp` (`add`), `nat/ops.disp` (`pred`, `is_zero`, `double`), `list.disp`, `set.disp`, `fin.disp`, `option.disp`, `result.disp`, `either.disp`, `stream.disp`, `pair.disp` (typed pairs are 2-field records via `make_pair`; `.fst`/`.snd` projection is uniform over concrete records and hyps — prelude fork-pairs remain the raw substrate idiom), `build.disp` (the record builders `build`/`build_curried`/`constructor_type`), `hbin.disp` (Tarau hereditarily-binary naturals), `oeq.disp` (observational equivalence as a type's FOURTH face + the guard policies `license_guard`/`guard_eq`/`freeze` — owning names at a relation: `setoid_of` derives a setoid from the cells — pointwise at Pi = funext-by-definition, componentwise at non-dependent records, `Eq` at data — overridable via a `{ setoid := ... }` record in `behavioral_specs` = quotients; `oeq`/`oeq_refl`/`oeq_sym`/`oeq_trans` projections, `LinkedPi` relational binder, `image_setoid` quotient-by-observation; deliberately NO elimination into arbitrary motives — funext-with-J is inconsistent against `tree_eq`; consumed via instantiate+Eq-lemmas, LinkedPi respect witnesses, and licensed replacement where transport = identity + use-site re-check; pins in `oeq.test.disp` + the motive-extensionality probe in `ext_gate_proto.test.disp`). (Records live in the kernel: the §2.6 cut — `make_record`/`field`/`Record`.)
- `tests/` — all `*.test.disp` files. Test runner globs recursively under `tests/`.

### Host tests
- `test/disp.test.ts` — vitest harness; recursively globs `lib/tests/**/*.test.disp`.
- `test/parser.test.ts` — parser unit tests.
- `test/tree.test.ts` — tree calculus runtime tests.

## Implementation status

**The codebase implements the two-Σ-op kernel of `TYPE_THEORY.typ`.** Kernel surface: `hyp_reduce` + `bind_hyp` + the dispatcher `param_apply` over a fixed Σ; `eliminator_frame` is the library `elim`; Pi/Bool/Nat/Eq/Type/Ord/Sigma/Refinement/Intersection/Coproduct/Record are ordinary library types across the kernel fragments (the kernel is split `cut`/`engine`/`cells`/`base`/`positive`/`generic`/`universe`; see Code layout). The cutover from the legacy 7-primitive shape has landed (`lib/types/` and `lib/kernel/{handlers,walker}.disp` are gone). The walker + Σ routing run in-language (no native dispatcher fast-path); the soundness carve-outs — root-sig reads + `tree_eq` only, stem-forge and neutral-triage rejected — are pinned by `lib/tests/soundness.test.disp`. **Beyond the spec:** telescopes (dependent n-ary records + derived fields; `KERNEL_DESIGN.md` § Telescopes) — `Action` was re-cut to the stuck/computes dichotomy (`Extend type | Reduce value`; the verdict case is written `Reduce (Ok v)` — the former `Return` alias is deleted), surface record-type literals `{ a : Nat, b := double a }` compile to `Telescope`, recValue fields scope sequentially with Rust-style puns, and `Sigma`/`Record` are telescope instances — pinned in `lib/tests/telescope.test.disp`. (`TYPE_THEORY.typ` has caught up on the two-tag `Action` and Telescope-subsuming-Sigma — its §54/§286/§3176 describe the current shape.)

### Status

**The code is the status, not this file.** For what's *landed*, read `git log`, `npm test`, and the kernel/std sources — the architecture is described under "Code layout" above; don't maintain a duplicate checklist here (it rots on every commit). For what's *not yet built*, the authoritative tracker is `TYPE_THEORY.typ` (by § number) plus the characterization tests in `lib/tests/soundness.test.disp`. Known-deferred at a glance: route-to-registered handler (§5.4 — partially realized: the §7A policed token IS one registered route, and do_check's param_apply routing rides it); `RecognizerShape` + the STRUCTURAL deep ascription `respond : RespondShape` in MetaShape (§11–§12 — the BEHAVIORAL respond check LANDED 2026-07-01 as `Type := BehavioralType`, GoodRespond in the universe; MetaShape fields stay loosened to `Tree`); effects `Eff R X` + driver (§15); cubical `Path` / `comp` / `transp` / `Glue` (§13); `strip` / erasure (§10); `wf_fix` / `Total`; richer `CheckerError` vocabulary + source-span diagnostics (currently one `Err`). **Landed (was deferred):** (1) `bind_hyp` body-walking (§7.2) — `bind_hyp` is merged into `param_walker` (`w_bind_hyp`), so the body runs UNDER the walker via the walker's own `self`; raw reflection in a body is rejected (the metadata-extraction leak is closed), and the escape-only `occurs` is no longer the sole guard. (2) Module typed exports auto-verify through the kernel at elaboration time (`verify mod` = `param_apply typ record`); raw recognizer application (`typ record`) bypasses the walker, so verification must go through `param_apply`/`verify`. A type whose recognizer mints a hyp (Pi/Intersection) applied raw fails fast. (3) The kernel's structural types are first-class and exported — `Tree`, `Frame`, `NeutralMeta`, `Action`, `RespondShape`, `MetaShape` (§11.2) — and the kernel self-types `make_meta : Tree -> Tree -> MetaShape` and `inert_respond : RespondShape` (checked by `verify` of each kernel fragment in `use_raw.test.disp`). **RespondShape ascription status (pinned in `lib/tests/metashape.test.disp`):** responds no longer raw-read neutral metas (hyp_reduce extracts once and passes `(params, self, frame)`); with params PINNED and frames STRUCTURED, the projecting family inhabits its per-former shapes (`InductiveRespondShape`/`ProjectingRespondShape`, incl. recursive gated Nat). The FLAT deep ascription `respond : RespondShape` in MetaShape stays open on unstructured `params`/`frame` (and behavioral correctness went to `GoodRespond`/R6 instead). **Typed meta access landed 2026-07-03:** `meta_of : Type -> MetaShape` reads a type's metadata under annotations via the `@meta` frame arm of the universe respond — a concrete type gives its real meta, a type-hyp a stuck MetaShape whose projections land their field types; the sanctioned-projection line on NEUTRALS stays `neutral_type` only — the payload's SHAPE is typed now (`Spine`, with `Neutral` for the parent slot), but reading a neutral's payload stays unsanctioned (the extraction leak).

## Compiler workarounds

Three issues affect kernel-level code involving recursion or multi-line conditional dispatch:

- **Match arm bodies are single-line.** A multi-line match arm body like `false => triage \n (arg1) \n (arg2)` parses only the bare `triage` because the arm body uses `lineExpr`. Wrap multi-line content in parens to make it one atom: `false => (triage \n (arg1) \n (arg2))`. Same workaround for multi-line `let` bodies.
- **`select_lazy` + self-recursion blows the compile-time budget.** A thunk `{_} -> self meta (pair_snd x)` compiles to `K body`. After outer bracket abstraction, `cirToTree`'s reduction of the closed combinator eagerly evaluates internal apps via `applyTree` (10M-step budget). For self-referential expressions, this fires fix-unfolding at compile time even though runtime semantics would short-circuit via `select_lazy`'s lazy thunk dispatch. **Workaround**: use `if`/`match` instead of `select_lazy` for bodies containing recursive calls — both close their branches/arms over the free-var union (`if`: `cond c ({fvs}->e1) ({fvs}->e2) fvs…`; `match`: `(λfvs. prod table c) fvs…`), side-stepping the eager K-body evaluation.

- **The η/saturation rule for match arms.** Every call in a match-arm body must carry at least one ARM-BOUND variable in its application spine. A call saturated by free-var-row variables alone (e.g. `self env e` where all three are bound outside the match) η-exposes under bracket abstraction (`[e](self env e) → self env`), and the cut's free-var distribution then EVALUATES it at runtime — for every arm, selected or not; if the call is recursive, that's an infinite regress. Calls ending in an arm-bound var stay partial during distribution (the standard `self name f` shape is safe), and `if`-guarded bodies are safe (cond selects before the branch row applies). When an arm needs the scrutinee itself, REBUILD it from the arm binders — hash-consing makes the rebuilt application identical to `e` (the deleted `lib/elab/compile.disp`'s `expr_to_cir` App/RecType arms were the worked example; recover via git).

- **The `bind_hyp` continuation must be an INLINE lambda in the recursive walker.** A continuation `{h} -> self … h` that is passed THROUGH a function (as a `let go` reused at the `bind_hyp` site, OR as a parameter to a helper that calls `bind_hyp ty go`) miscompiles under *nested* binders: the fresh hyp leaks into the result and trips `bind_hyp`'s occurs-check, so a function whose body uses an OUTER argument (`{a}->{b}-> t a b`) wrongly rejects while `{a}->{b}->b` passes. Inline the lambda directly in the `bind_hyp` call (`bind_hyp (e.ty) ({h} -> self … h)`), never a variable bound elsewhere. The applied uses of the same continuation (`go x`, `go recipe`) are fine — only the *unapplied pass to `bind_hyp`* breaks. This is why the telescope `tele_walk` walker handles `mint` inline and the per-cell ops return a *Step* (data) for `tele_walk` to interpret, rather than the ops calling `bind_hyp` with a passed `kont` (the unification debugging, 2026-06).

(Retired 2026-06-12: the **closed-prefix-redex hazard** — a match-arm recursive call `self name x` whose prefix `app(self, name)` became a closed redex that `cirToTree` unfolded forever, requiring `wait self name x`. The match desugar now closes the whole cut over the arms' free vars, so arm-body recursion stays open under bracket abstraction; the `wait` workaround is no longer needed in match arms. The η/saturation rule above is the surviving RUNTIME cousin — it predates the desugar change and applies equally to the old shape.)

## Key tree-calculus idioms

- **`wait` for deferred application.** `wait a b c = a(b)(c)` but `wait(a)(b)` doesn't evaluate `a(b)`. Essential for `fix` and partial application.
- **Plain `fix` handlers.** The kernel's Σ-ops (`hyp_reduce`/`bind_hyp`) are plain `fix`-forms. The old `(ks, raw, query)` self-proxy handler protocol and the `rec`/`recq` self-referential-record combinators were retired with the 7-primitive kernel (nothing in the new kernel used them).
- **Wait-based types.** `wait(checker)(metadata)`. Signature = `pair_fst(T)` (constant per checker). Metadata = `type_meta(T) = pair_snd(pair_snd(T))`, a §2.6 record whose fields (`respond`, `recognizer_params`, …) are read **by name** via the cut (`m.respond`, `m.recognizer_params`). Type-former recognition is via signature comparison.
- **H-rule via `make_recognizer`.** The universal recognizer-side H-rule lives in `make_recognizer`, which reconstructs the type via `wait (wait wrap body) meta` and short-circuits `Ok true` when the candidate is a neutral of that stored type. `type_predicate_h_rule` is the predicate-side dual.
- **Hash-consing is load-bearing.** Conversion is `tree_eq`, an O(1) hash-cons identity check. Deterministic elaboration ensures same type → same tree.
- **Native fast-paths.** `tree_eq` is a native fast-path in `src/core/tree.ts`, producing bit-identical results to the in-language reference; the in-language code is the spec. The dispatcher (`param_apply`) runs in-language — no native fast-path (the legacy native dispatcher was removed in the cutover).

## Testing

`npm test` runs vitest. `lib/tests/**/*.test.disp` is the primary object-language suite. `test/parser.test.ts` and `test/tree.test.ts` cover the host infrastructure.

## Operating notes

- **Git workflow — work on `main` directly.** Do all work on `main` and push to `origin/main`; do NOT create feature/topic branches (or branch "to be safe") unless the user explicitly asks. This overrides the default "if on the default branch, branch first" caution for this repo. (Isolated agent worktrees for parallel subagents are a separate tooling concern and remain fine.)
- Type checking is raw `apply(T, v) = true`. Types are wait-based raw functions.
- `is_neutral` is an O(1) signature check (`pair_fst v` against `hyp_sig`); `is_hyp_fork` adds the fork-shape guard where partial wait-forms could share the bare signature.
- Prefer editing existing files over creating new ones.
- Binder parameter names shadow scope variables during compilation. Name collisions are safe but should be avoided for clarity.
- Files with any `name := expr` field use the export model where only `:=` fields export. Files with only `let`/`test`/`open` use legacy mode where all `let` bindings export. Prefer `name := expr` for new code.
- **Declarations are guard-mediated** (COMPILATION.typ § Declarations as requests): `guard <policy> NAME : T := v` owns a name; rebinding an owned name is an ordinary declaration whose payload carries credentials (`{ new := …; proof := … }` for `license_guard`); `guard_eq NAME : T := v` derives the license from the annotation. Unguarded duplicate fields error in the DRIVER now (the parser accepts redefinition syntax). Guard-free code takes a fast path that is byte-identical to the pre-guard behavior.
- **Modules are HERMETIC with explicit dependencies** (landed 2026-07-06, MODULES.md). A used file sees ONLY its own defs + opens + givens — never the use site's scope (the module cache keys `(path, raw?, fillKey)`; policy shadowing is file-local; a missing import errors naming the file). `given X : T (:= default)?` declares a module dependency (the `given` decorator in cut.disp; `Request.param`); the canonical spelling is the header block `open given { X : T, Y : U := d }` (parser sugar, one member per entry desugaring to the line form — order preserved, line form still parses); fills are EXPLICIT: `use "f" { x := v, puns }` (named-argument rules; at least one `:=` — all-pun records parse as recTypes). The fill binds before the given's annotation compiles (self-typed `given Type : Type` = R6). Bare `use` of a given-bearing module ERRORS (raw included) — the empty context is explicit: `use "f" {}` (checked: defaults apply; raw: unfilled givens are ABSENT from scope, referencing one errors unbound — kernel givens are annotation-only, so the fragments' raw sibling opens are `use raw "x.disp" {}`). Root files cannot declare givens. Dynamic (custom-decorator) givens are rejected. **Slice 2 (functor face) landed 2026-07-06:** bare CHECKED `use "f"` on a given-bearing module = the functor tuple (`record` = the readback lambda over the givens, `typ` = Pi-into-Record via the in-scope Pi route); `verify` of the tuple = the one abstract check (givens minted as hyps); replay (`(use "f").record fill…`) is tree-identical to template instantiation — readback rebuilds hyp-bearing EXTENSION neutrals as the application that created them, so partially-applied deps and record-given projections replay exactly. Modules the abstract pass can't elaborate (self-typed givens — the kernel fragments) fall back to the explicit-context error; bare RAW use always errors. Pins: given.test.disp + modules.test.ts. Slice 3 (verify-once dividends, partial fills, module guards) remains.
- **`let` and `test` are NOT keywords** (landed 2026-07-05). `let x := e` is a decorated declaration whose head is the library value `let` (cut.disp, `private := true`); the driver fast-paths while `let` is unbound (bootstrap) or pristine, and a shadowed `let` decorator actually runs. Legacy `let x = e` is REMOVED (targeted parse error). Tests are the equation item `lhs = rhs` (compound lhs required — bare `x = 5` rejected); `test` is the prelude identity, peeled while unbound/pristine (this restores the application head so named-arg calls in a test lhs resolve — keep the peel), run when shadowed. Braced `let` remains the lexical binding form, recognized structurally. Legacy all-lets-export mode is gone; a file with no own exports re-exports its opens (the barrel rule). Keywords now: `use open match if then else`.
